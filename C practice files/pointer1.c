// Program for introduction to pointers
// Pointers basically are used to store the address of the variables
// we can access any address of the variables using "&" operator

#include <stdio.h> 
  
int main() 
{ 
    int x, y, z;
    printf("Initial addresses : %p %p %p\n", &x, &y, &z); 
    return 0; 
}

